import React, { Component } from 'react';

class StatelessCC extends Component {
    render() { 
        return ( 
            <span>{this.props.title}</span>
         );
    }
}
 
export default StatelessCC;